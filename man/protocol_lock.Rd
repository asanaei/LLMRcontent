% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coder_protocol.R
\name{protocol_lock}
\alias{protocol_lock}
\title{Lock a protocol}
\usage{
protocol_lock(x)
}
\arguments{
\item{x}{A \code{\link[=protocol]{protocol()}}.}
}
\value{
The protocol, locked, with \verb{$hash} set.
}
\description{
Computes and stores a content hash covering the codebook, prompt template,
provider, model, generation parameters, \code{no_change}, embedding flag,
replicate count, and parser source and captured values. \code{\link[=validate_protocol]{validate_protocol()}}
on the sealed split and \code{\link[=code_corpus]{code_corpus()}} require a locked protocol and compare
its current content with the stored hash. Hashes use \code{LLMR::llm_hash()}.
}
\examples{
cb <- codebook("tone", "one sentence",
  list(cb_category("positive", "Approving."),
       cb_category("negative", "Critical.")))
p  <- protocol(cb, LLMR::llm_config("groq", "openai/gpt-oss-20b"))
protocol_lock(p)
}
