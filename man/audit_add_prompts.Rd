% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/valid_plan.R
\name{audit_add_prompts}
\alias{audit_add_prompts}
\title{Add prompt paraphrases}
\usage{
audit_add_prompts(plan, ...)
}
\arguments{
\item{plan}{An \code{\link[=audit_plan]{audit_plan()}}.}

\item{...}{Named prompt templates (same placeholder rules as the
baseline). Honest paraphrases ask the same question in different
words; do not "improve" the prompt here -- that is tuning, and it
belongs in the coding tournament, before the audit.}
}
\value{
The plan, extended.
}
\description{
Add prompt paraphrases
}
\examples{
plan <- audit_plan(data.frame(text = c("a", "b")), "text",
  function(d) mean(d$label == "yes"), c("yes", "no"), "{text}")
audit_add_prompts(plan, concise = "Classify this text: {text}")
}
