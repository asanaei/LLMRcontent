% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coder_run.R
\name{code_corpus}
\alias{code_corpus}
\title{Code a corpus with a locked protocol}
\usage{
code_corpus(corpus, protocol, text, .runner = NULL, id = NULL, ...)
}
\arguments{
\item{corpus}{A data frame.}

\item{protocol}{A \strong{locked} \code{\link[=protocol]{protocol()}}.}

\item{text}{Name of the text column in \code{corpus}.}

\item{.runner}{Internal seam for tests: a function \verb{(experiments, ...)}
returning the experiments with a \code{response_text} column. Default
\code{LLMR::call_llm_par()}.}

\item{id}{Optional name of a stable unit-identifier column in \code{corpus}. Carry
it through so \code{\link[=gold_correct]{gold_correct()}} can link audit units to corpus rows by id,
the only way to disambiguate rows that share identical text. Use the same
\code{id} here as in \code{\link[=gold_set]{gold_set()}}.}

\item{...}{Passed to the runner (e.g. \code{tries}, \code{progress}).}
}
\value{
\code{corpus} plus \code{label} (modal label), \code{label_share} (share of
replicates agreeing with it), \code{parse_failures} per unit, a \code{.text_hash}
linkage column, and when \code{protocol$replicates > 1} the individual replicate
columns \code{label_rep1}, \code{label_rep2}, ....
}
\description{
Applies the locked protocol to every text, with \code{protocol$replicates}
codings per unit. With replicates, the modal label and the share of
replicates agreeing with it are returned, which is the unit-level
stability diagnostic reviewers should ask for.
}
\details{
Execution is live and parallel (\code{LLMR::call_llm_par()}); the
\code{.runner} seam accepts any function with the same contract, including
the replayer \code{\link[=archive_replay]{archive_replay()}} returns.
}
\examples{
cb <- codebook("tone", "one sentence",
  list(cb_category("positive", "Approving."),
       cb_category("negative", "Critical.")))
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b", temperature = 0)
\dontrun{
p <- protocol_lock(protocol(cb, cfg, replicates = 2))
code_corpus(data.frame(text = c("clear progress", "serious problem")),
            p, "text")
}

# The `.runner` seam answers the calls without a provider, for tests or for
# a deterministic or external coder:
p <- protocol_lock(protocol(cb, cfg))
keyword_coder <- function(experiments, ...) {
  user <- vapply(experiments$messages, `[[`, "", "user")
  experiments$response_text <- ifelse(grepl("progress", user),
                                      "positive", "negative")
  experiments
}
code_corpus(data.frame(text = c("clear progress", "serious problem")),
            p, "text", .runner = keyword_coder)
}
