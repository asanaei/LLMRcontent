% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/valid_plan.R
\name{audit_plan}
\alias{audit_plan}
\title{Declare a measurement-robustness audit}
\usage{
audit_plan(data, text, estimator, labels, prompt)
}
\arguments{
\item{data}{The data frame whose texts are measured.}

\item{text}{Name of the text column.}

\item{estimator}{The estimand as code: a function that receives \code{data}
with one added character column \code{label} (the LLM measurement under one
grid cell) and returns a single numeric value -- a share, a regression
coefficient, a difference in means. Whatever number the paper reports,
this function computes it. Two contract points: \code{label} \strong{may contain
\code{NA}} (parse failures), and your estimator must decide what they mean
-- exclude, impute, or treat as a category -- explicitly; and if the
estimator errors in a cell, \code{\link[=audit_run]{audit_run()}} records \code{NA} for that cell
and counts it, rather than aborting the grid.}

\item{labels}{Valid labels, in their baseline order. The prompt may
reference them via the \code{{labels}} placeholder, which is what the
label-order perturbation reorders.}

\item{prompt}{Baseline prompt template; must contain \code{{text}}, may
contain \code{{labels}}. Placeholders are substituted literally, so braces
in the measured text are safe.}
}
\value{
An \code{audit_plan} with a single prompt variant (\code{"base"}), no
models, label order \code{"as_given"}, and temperature \code{0}. Add the grid
with \code{\link[=audit_add_models]{audit_add_models()}}, \code{\link[=audit_add_prompts]{audit_add_prompts()}}, \code{\link[=audit_add_perturbations]{audit_add_perturbations()}}.
}
\description{
Declare a measurement-robustness audit
}
\examples{
plan <- audit_plan(
  data = data.frame(text = c("cut taxes now", "fund the schools")),
  text = "text",
  estimator = function(d) mean(d$label == "conservative"),
  labels = c("conservative", "progressive"),
  prompt = "Classify the slogan as one of: {labels}.\n\n{text}\n\nLabel:"
)
plan
}
