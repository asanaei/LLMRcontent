% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install_gui_deps.R
\name{install_gui_deps}
\alias{install_gui_deps}
\title{Install the LLMRcontent GUI's suggested packages}
\usage{
install_gui_deps(upgrade = FALSE, quiet = FALSE)
}
\arguments{
\item{upgrade}{If \code{FALSE} (default), packages already installed are left
alone; if \code{TRUE}, CRAN packages are reinstalled to their latest versions.}

\item{quiet}{If \code{TRUE}, suppress the per-package progress messages.}
}
\value{
Invisibly, a named logical vector: \code{TRUE} where the package is
available after the call, \code{FALSE} where it could not be installed (e.g.
LLMR.shiny when it is not on CRAN and not already installed).
}
\description{
The Shiny GUI (\code{\link[=run_content_studio]{run_content_studio()}}) needs five suggested packages: shiny,
bslib, DT, ggplot2, and LLMR.shiny. This installs the ones that are missing.
The first four come from CRAN; LLMR.shiny is the family's shared GUI
substrate and may not be on CRAN yet, in which case this points you to its
GitHub source rather than failing.
}
\examples{
if (interactive()) {
  install_gui_deps()
}
}
