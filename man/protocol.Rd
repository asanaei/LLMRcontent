% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coder_protocol.R
\name{protocol}
\alias{protocol}
\title{Assemble a coding protocol}
\usage{
protocol(
  codebook,
  config,
  prompt = NULL,
  parser = NULL,
  replicates = 1L,
  label = NULL
)
}
\arguments{
\item{codebook}{A \code{\link[=codebook]{codebook()}}.}

\item{config}{An \code{LLMR::llm_config()} for a generative model. For
annotation work prefer \code{temperature = 0}.}

\item{prompt}{Prompt template containing the placeholder \code{{text}} and,
optionally, \code{{codebook}} (replaced by \code{\link[=format_codebook]{format_codebook()}}'s rendering).
\code{NULL} uses a sensible default. Placeholders are substituted literally,
so braces in the coded text itself are safe.}

\item{parser}{A function \verb{(text, labels) -> label} turning a model reply
into one of the codebook's labels (or \code{NA}). \code{NULL} uses the package's
label-matching parser.}

\item{replicates}{How many times each unit is coded before its modal label is
used by \code{\link[=tune_protocol]{tune_protocol()}}, \code{\link[=validate_protocol]{validate_protocol()}}, and \code{\link[=code_corpus]{code_corpus()}}
(replicates make model self-disagreement measurable; see
\code{\link[=coder_agreement]{coder_agreement()}}).}

\item{label}{Optional human-readable tag used in tournaments and reports
(e.g. \code{"v2-fewshot-qwen"}); defaults to provider/model.}
}
\value{
A \code{coding_protocol} (unlocked).
}
\description{
Assemble a coding protocol
}
\examples{
cb  <- codebook("tone", "one sentence",
  list(cb_category("positive", "Approving."),
       cb_category("negative", "Critical.")))
cfg <- LLMR::llm_config("groq", "openai/gpt-oss-20b", temperature = 0)
p   <- protocol(cb, cfg)
protocol_lock(p)
}
\seealso{
\code{\link[=protocol_lock]{protocol_lock()}}, \code{\link[=tune_protocol]{tune_protocol()}}, \code{\link[=validate_protocol]{validate_protocol()}}
}
