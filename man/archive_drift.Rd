% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/archive_contracts.R
\name{archive_drift}
\alias{archive_drift}
\title{Measure drift in archived calls against the live providers}
\usage{
archive_drift(
  archive,
  fraction = NULL,
  n = NULL,
  strata = c("provider", "model"),
  .runner = NULL,
  ...
)
}
\arguments{
\item{archive}{An \code{archive}.}

\item{fraction}{Fraction of eligible calls to re-issue within each stratum,
in \verb{(0, 1]}. Mutually exclusive with \code{n}.}

\item{n}{Total number of eligible calls to re-issue, allocated across
strata. Mutually exclusive with \code{fraction}.}

\item{strata}{Manifest columns to stratify by.}

\item{.runner}{Offline seam: a function taking an experiments data frame
with \code{config} and \code{messages} list-columns and returning those rows with at
least a \code{response_text} column. Default \code{LLMR::call_llm_par()}.}

\item{...}{Passed to the runner.}
}
\value{
An \code{archive_drift} object: \code{table} is a per-stratum tibble that
begins with the chosen \code{strata} columns (\code{provider}, \code{model} by default),
followed by \code{n_eligible}, \code{n_sampled}, \code{n_exact}, \code{exact_rate},
\code{n_temperature0}, \code{n_version_changed}; \code{details} is a per-record
tibble (\code{idx}, \code{provider}, \code{model}, \code{temperature}, \code{exact},
\code{archived_version}, \code{served_version}).
}
\description{
Draws a stratified sample of archived calls, re-issues them, and reports
exact-match rates and any change in the served \code{model_version}. Exact
reproduction is expected only for temperature-zero calls on pinned
open-weight backends; for sampled calls disagreement is sampling, not
necessarily drift, and the print method says so.
}
\details{
\code{fraction} and \code{n} are mutually exclusive. When neither is supplied,
\code{fraction = 0.05} is used. A fraction draws \code{ceiling(fraction * n)} records
from each stratum. A count is allocated across strata by largest remainder
in proportion to stratum size, with at least one record per nonempty stratum
when the count allows. The draw uses \code{\link[=sample]{sample()}}; set a seed beforehand for a
reproducible sample. Eligibility, request reconstruction, and archived text
are read from the stored raw records, not their parsed cache.
}
\examples{
log <- tempfile(fileext = ".jsonl")
writeLines(paste0('{"ts":"2026-06-01T10:00:01+0000","schema_version":"1.0",',
  '"kind":"call","provider":"openai","model":"gpt-4o-mini","status":200,',
  '"request":{"messages":[{"role":"user","content":"Capital of France?"}],',
  '"temperature":0},"model_version":"gpt-4o-mini-2026-06-01",',
  '"usage":{"sent":5,"rec":1},"response_id":"r-1","text":"Paris"}'), log)
a <- archive_build(log)

# Live use issues real calls; here a runner that echoes the archived text
# stands in through the .runner seam so the example needs no key.
echo <- function(experiments, ...) {
  experiments$response_text <- "Paris"
  experiments$model_version <- "gpt-4o-mini-2026-06-01"
  experiments
}
archive_drift(a, fraction = 1, .runner = echo)$table
}
