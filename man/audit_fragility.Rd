% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/valid_run.R
\name{audit_fragility}
\alias{audit_fragility}
\title{How small a measurement change flips the conclusion?}
\usage{
audit_fragility(audit, reference = 1L, flip = sign_flip())
}
\arguments{
\item{audit}{An \code{\link[=audit_run]{audit_run()}} result.}

\item{reference}{Reference cell (default 1).}

\item{flip}{A flip rule (see \link{flip_rules}); default \code{\link[=sign_flip]{sign_flip()}}.}
}
\value{
Integer (or \code{Inf}), with the flipping cells in a
\code{flipping_cells} attribute. When the reference cell's estimate is NA the
value is \code{Inf} with a \code{status = "reference_failed"} attribute, since a
failed reference has no sign or threshold to flip against.
}
\description{
The fragility index is the smallest number of grid dimensions (prompt,
model, label order, temperature) that must change from the reference
cell before the conclusion -- as defined by the \code{flip} rule -- changes.
\code{Inf} when no cell in the grid flips, which is a statement about this
grid, not a proof of robustness.
}
