% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coder_codebook.R
\name{codebook}
\alias{codebook}
\title{Create a codebook}
\usage{
codebook(name, unit, categories, instructions = NULL, version = "1.0")
}
\arguments{
\item{name}{Short name of the construct (e.g. \code{"populist framing"}).}

\item{unit}{What one coding unit is (e.g. \code{"one parliamentary speech"}).}

\item{categories}{A list of \code{\link[=cb_category]{cb_category()}} objects. Labels must be unique.}

\item{instructions}{Optional free-text instructions placed before the
category definitions (coder-neutral: the same text serves human coders).}

\item{version}{A version string you control (e.g. \code{"1.2"}); bump it when
the instrument changes. The content hash changes with any edit either
way; the version is for humans.}
}
\value{
A \code{codebook} object.
}
\description{
A codebook is the measurement instrument of a content analysis: construct
definitions, category boundaries, decision rules, and examples. Here it is
a first-class object -- versioned, printable, hashable -- that compiles
into the prompt via \code{\link[=format_codebook]{format_codebook()}}, so the instrument the model saw
is exactly the instrument in the paper's appendix.
}
\examples{
cb <- codebook(
  name = "populist framing",
  unit = "one speech",
  categories = list(
    cb_category("populist",     "People-vs-elite framing is present."),
    cb_category("not_populist", "No people-vs-elite framing.")
  ),
  version = "1.0"
)
cb
codebook_hash(cb)
}
\seealso{
\code{\link[=cb_category]{cb_category()}}, \code{\link[=format_codebook]{format_codebook()}}, \code{\link[=protocol]{protocol()}}
}
