% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coder_gold.R
\name{gold_size}
\alias{gold_size}
\title{Plan the size of a gold set}
\usage{
gold_size(
  expected_agreement = 0.85,
  ci_width = 0.1,
  conf = 0.95,
  n_grid = c(50, 100, 200, 300, 500, 800),
  sims = 2000
)
}
\arguments{
\item{expected_agreement}{Anticipated model-gold agreement (default 0.85).}

\item{ci_width}{Target total width of the 95\% interval (default 0.10).}

\item{conf}{Confidence level (default 0.95).}

\item{n_grid}{Candidate sizes to evaluate.}

\item{sims}{Monte Carlo draws per candidate size.}
}
\value{
The smallest n in \code{n_grid} meeting the target, with the simulated
widths as an attribute.
}
\description{
Answers, by simulation, the budgeting question every project starts with:
how many human-labeled units do I need so that the agreement estimate
(proportion of model labels matching gold) has a confidence interval no
wider than \code{ci_width}? The simulation draws from a binomial at the
anticipated agreement level, which is adequate for planning; report the
realized interval from \code{\link[=validate_protocol]{validate_protocol()}} in the paper.
}
\examples{
set.seed(110)
gold_size(expected_agreement = 0.85, ci_width = 0.10)
}
